# Spring Shell Configuration
spring.shell.interactive.enabled=true
spring.shell.history.enabled=true
spring.shell.history.name=.spring-shell-demo.history
  
  # Banner
spring.main.banner-mode=console
  
  # Application name shown in shell prompt
spring.application.name=demo-shell